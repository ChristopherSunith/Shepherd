#ifndef AUTHENTICATOR_H
#define AUTHENTICATOR_H

/*
 *Primary function of this
 *class is to Authenticate
 *This acts as an interface to an implementation class which is not yet implemented
 *This interface will allow the user to be authenticated without the knowledge of how
 */

class Authenticator
{
public:
    Authenticator();
};

#endif // AUTHENTICATOR_H
